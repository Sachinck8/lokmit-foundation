# LOKMIT FOUNDATION frontend - local development environment variables.
# Copy this file to ".env" (gitignored) only if you need to override defaults.
# Base URL of the backend API. During `npm run dev` the Vite dev server
# already proxies /api -> http://localhost:8080.
VITE_API_BASE_URL=/api/v1