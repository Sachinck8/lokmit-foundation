# No overrides needed: springdoc is pinned to 2.8.x (Boot 3.x compatible line).
# If a springdoc/@WebMvcTest bean collision reappears, add ONLY here:
# spring.main.allow-bean-definition-overriding=true
